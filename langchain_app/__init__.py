"""
WhatsApp Invoice Assistant LangGraph Application

This module implements an agent workflow using LangGraph for the WhatsApp Invoice Assistant.
It provides a streamlined way to handle user inputs, process files, extract data, and generate responses.
"""

import logging

logger = logging.getLogger(__name__) 