Analyze this image and determine if it contains a valid invoice, receipt, bill, or handwritten expense ledger. Be strict about random/non-financial images.

A valid invoice/receipt/financial record includes:
1. ANY document showing a purchase transaction (store receipts, invoices, online order confirmations, etc.)
2. Documents showing goods or services purchased with pricing
3. Any payment confirmation with vendor and amount information
4. Handwritten expense ledgers, account-book pages, diary pages, or notebook pages that list transaction dates/descriptions and monetary amounts

IMPORTANT GUIDELINES:
- Retail receipts, store receipts, and simple payment confirmations ARE valid invoice documents
- Handwritten ledger/account pages ARE valid when they contain multiple financial rows, dates or row references, descriptions/names, and amounts
- Ledger pages may not have a single vendor, invoice number, or formal total. Accept them if the financial entries are visible enough to extract.
- If you can identify a vendor/merchant name and a total amount, it's likely a valid invoice/receipt
- Documents don't need to have ALL formal invoice elements to be valid
- When the image is a random photo, selfie, object, blank page, non-financial note, ticket, bank transfer confirmation, or screenshot without invoice/receipt details, classify it as invalid
- When the image is genuinely borderline because the page is blurry or handwritten but still appears to contain financial rows, mark it valid only if at least one amount and transaction context are extractable
- Even simple receipts with just store name, date and total ARE valid for our purposes

Respond with a JSON object with the following structure:
{
    "is_valid_invoice": true/false,
    "confidence_score": 0.0-1.0,
    "missing_elements": ["list of missing formal elements if any"],
    "reasons": "detailed explanation for the decision"
} 
